import { HoverBorderDirectiveDirective } from './hover-border-directive.directive';

describe('HoverBorderDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new HoverBorderDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
